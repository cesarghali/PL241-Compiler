# While loop code generation test
# Also ensures code gen can handle constants in the condition
main
var i, j;
{
	let i <- 0;
	let j <- call InputNum();
	while i < 10 do
		call OutputNum(i);
		let i <- i + 1;
		call OutputNum(j);
		let j <- j * 2;
		
		call OutputNewLine()
	od
}.